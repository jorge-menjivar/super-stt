**/target
**/venv
**/dist
**/build
**/__pycache__
**/*.env
**/*.log
*.mp3
*.wav
*.flac
*.ogg
!backends/whisper/tests/data/*.wav

.claude/worktrees/
.claude/scheduled_tasks.lock

# Coverage output. CI renders these into a throwaway runner and uploads them;
# running the same recipes locally writes them to the repo root.
lcov.info
coverage-html/

# Generated from the daemon's router by `just openapi`; CI generates and
# publishes it, so it is never committed.
docs/protocol/openapi.json

docs/design/
docs/superpowers/
.superpowers/
